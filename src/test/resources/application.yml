spring:
  application:
    name: my-blog-back-app-test
  datasource:
    driver-class-name: org.h2.Driver
    url: jdbc:h2:mem:blog_test;DB_CLOSE_DELAY=-1;MODE=PostgreSQL;DATABASE_TO_LOWER=TRUE
    username: sa
    password:
  sql:
    init:
      mode: always
      schema-locations: classpath:schema.sql
